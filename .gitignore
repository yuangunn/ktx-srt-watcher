__pycache__/
*.pyc
.venv/
.env
.DS_Store
node_modules/
dist/
.pytest_cache/
*.egg-info/
.claude/launch.json


# Watch list and poll state live in the CF Worker's KV, never in git — the
# watch ids embed routes and travel dates and this repo is public.
config.json
state.json
