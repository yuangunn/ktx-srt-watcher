# pykorail speaks the 코레일+ protocol (post 2026-09-01 merger): the backend
# fingerprints the TLS handshake, so it rides on curl-cffi impersonation —
# the reason patching the old korail2 could never work. MIT, on PyPI.
# SRTrain is gone with the SRT service itself (app and API closed 2026-08-31).
pykorail>=0.2.0
requests>=2.31.0
pydantic>=2.5.0
pytest>=8.0.0
