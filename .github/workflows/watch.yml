name: ticket-watch

on:
  schedule:
    # CF Worker bridge (cloudflare-worker/) is the primary cron at the
    # rate set in wrangler.toml. This schedule is just a low-frequency
    # fallback so the watcher still ticks if CF Worker is broken
    # (PAT expired, account suspended, etc.). Each GHA run bills at
    # least 1 min regardless of duration, so we keep this sparse —
    # 4 runs/day fits comfortably under the 2,000 min/month free tier
    # alongside the CF-driven runs.
    - cron: '0 */6 * * *'
  workflow_dispatch:                   # PWA "지금 확인" / 수동 실행
    inputs:
      test_notify:
        description: '알림 경로 테스트 (조회/예약 없이 알림만 발송)'
        type: choice
        options: ['off', 'all', 'telegram', 'pushover']
        default: 'off'
  repository_dispatch:
    types: [cron-tick]                 # CF Worker 정확한 cron

# 이 워크플로는 더 이상 저장소에 쓰지 않습니다. config/state는 CF KV에 있고,
# PWA에서 config를 저장해도 커밋이 없으므로 push 트리거도 없앴습니다 —
# 변경은 다음 정규 폴링에서 반영됩니다.
permissions:
  contents: read

concurrency:
  # cancel-in-progress: a newer tick supersedes an older queued one. With CF
  # firing every 5 min, queuing (false) let runs pile up and hit the 15-min
  # queue limit → cancelled (looked like "failed"). State is reconciled by the
  # next run's semantic merge, so cancelling a superseded run is safe.
  group: ticket-watch
  cancel-in-progress: true

jobs:
  watch:
    # Home device, not a GitHub-hosted runner: the 코레일+ backend refuses
    # datacenter IPs outright (code -8202, "VPN 또는 데이터센터를 통해서는
    # 서비스를 이용할 수 없습니다"), so polling from Azure can never log in.
    # Residential egress is a hard requirement, not an optimization.
    # Setup: docs/SELF_HOSTED_RUNNER.md. While the runner is offline, ticks
    # queue and the concurrency group keeps only the newest — no pile-up.
    runs-on: [self-hosted, korail-home]
    timeout-minutes: 8        # cron 간격보다 짧게
    steps:
      - uses: actions/checkout@v4

      # Cheap pre-install gate (stdlib only, no pip). Most ticks are throttled
      # skips; deciding here lets us bypass the ~15s install + poll, so the
      # concurrency queue never backs up.
      - name: Poll gate
        id: gate
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          CF_WORKER_URL: ${{ secrets.CF_WORKER_URL }}
          REMINDER_TOKEN: ${{ secrets.REMINDER_TOKEN }}
        # python3 is preinstalled on ubuntu-latest; the gate needs no pip deps
        # (worker.remote is stdlib-only for exactly this reason).
        run: python3 -m worker.gate >> "$GITHUB_OUTPUT"

      # System python + a local venv instead of actions/setup-python: on a
      # self-hosted ARM device (Raspberry Pi) setup-python has no prebuilt
      # interpreters to download, and the device's python3 (>=3.10 for
      # pykorail) is exactly what we want anyway. The venv persists in the
      # runner's work dir, so installs after the first are no-ops.
      - name: Install deps
        if: steps.gate.outputs.poll == 'true' || (github.event_name == 'workflow_dispatch' && inputs.test_notify != 'off')
        run: |
          python3 -m venv .venv
          .venv/bin/pip install --quiet -r requirements.txt

      # Fires the real seat-found alert with a fabricated train. The path that
      # matters only runs when a cancellation appears — which is exactly when
      # you cannot afford to find out it is broken.
      - name: Test notification
        if: github.event_name == 'workflow_dispatch' && inputs.test_notify != 'off'
        env:
          TZ: Asia/Seoul
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          PUSHOVER_TOKEN: ${{ secrets.PUSHOVER_TOKEN }}
          PUSHOVER_USER: ${{ secrets.PUSHOVER_USER }}
          CF_WORKER_URL: ${{ secrets.CF_WORKER_URL }}
          REMINDER_TOKEN: ${{ secrets.REMINDER_TOKEN }}
        run: .venv/bin/python -m worker.main --test-notify=${{ inputs.test_notify }}

      - name: Run watcher
        # A test run must not also poll: it exists to isolate the alert path.
        if: steps.gate.outputs.poll == 'true' && (github.event_name != 'workflow_dispatch' || inputs.test_notify == 'off')
        env:
          TZ: Asia/Seoul
          KORAIL_ID: ${{ secrets.KORAIL_ID }}
          KORAIL_PW: ${{ secrets.KORAIL_PW }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Optional. Unset = Telegram only (which cannot wake a silenced phone).
          PUSHOVER_TOKEN: ${{ secrets.PUSHOVER_TOKEN }}
          PUSHOVER_USER: ${{ secrets.PUSHOVER_USER }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          CF_WORKER_URL: ${{ secrets.CF_WORKER_URL }}
          REMINDER_TOKEN: ${{ secrets.REMINDER_TOKEN }}
        run: .venv/bin/python -m worker.main
